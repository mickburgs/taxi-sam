{% if form is defined and form is not empty %}

    <div class="col-xs-12 form">
        <h2 class="title">{{ 'make_appointment'|trans }}</h2>

        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

{% endif %}